/*
- Auth with GitHub and Vercel
- Create a Github repo
- Copy files in {repo} (from api)

*/
export const POST = async (request: Request) => {
  //1) repos/create-for-authenticated-user

  return new Response("404");
};
